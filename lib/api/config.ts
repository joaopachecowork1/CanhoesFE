/**
 * Shared API configuration constants.
 *
 * API_TIMEOUT_MS is intentionally matched between the proxy (server-side) and
 * canhoesClient (browser-side) so requests time out consistently end-to-end.
 */
export const API_TIMEOUT_MS = 6_000;
